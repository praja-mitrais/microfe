{
  "name": "hrm",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:host": "yarn --cwd \"./hrm-host\" dev",
    "dev:employee": "yarn --cwd \"./hrm-employee\" dev",
    "stop": "kill-port --port 5000,5001"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.3.2",
    "kill-port": "^2.0.1"
  }
}
